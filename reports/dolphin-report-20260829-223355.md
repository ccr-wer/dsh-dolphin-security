# Dolphin 主动巡检报告

- 扫描目录：`D:\Dolphin`
- 规则集：`p/security-audit`
- 生成时间：2026-08-29T14:33:55.978Z
- 发现问题：共 3 处（ERROR 2 / WARNING 1 / INFO 0）

## demo/app.py（1 处）

- **[ERROR]** 第 30:9 行 · 规则 `python.lang.security.audit.sql-injection.sql-injection`：Detected SQL statement that is tainted by user input.

  ```
      cur.execute("SELECT * FROM users WHERE id=" + uid)
  ```
  - 修复建议：参考 CWE-89

## demo/vuln.py（1 处）

- **[ERROR]** 第 12:5 行 · 规则 `python.lang.security.audit.dangerous-system-call.dangerous-system-call`：Found user-controlled data in os.system() call. This could be vulnerable to command injection.

  ```
      os.system("rm -rf " + user_input)
  ```
  - 修复建议：使用 subprocess.run 并显式传入参数列表，避免 shell 拼接用户输入。

## demo/hash.js（1 处）

- **[WARNING]** 第 8:1 行 · 规则 `generic.ci.security.use-of-md5.use-of-md5`：Detected use of the weak hash function MD5.

  ```
  const h = crypto.createHash('md5')
  ```
  - 修复建议：参考 CWE-328
